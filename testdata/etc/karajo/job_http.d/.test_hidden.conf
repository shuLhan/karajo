[job.http "Test hidden"]
description = The job to test what the hidden config.
secret = s3cret
interval = 20s
max_running = 1
http_method = POST
http_url = /karajo/api/job/run/test-hidden
http_request_type = json
http_header = X: Y
http_insecure = false
